import { type Resolver } from "nuxt/kit";
import type { PageInterface } from "../../types/index.js";
declare const _default: () => {
    pageLoader: (resolver: Resolver, baseDir?: string, prefix?: string, dir?: string) => PageInterface[];
};
export default _default;
